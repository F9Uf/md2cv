---
phase: quick
plan: 260415-05x
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Editor.tsx
autonomous: true
must_haves:
  truths:
    - "The CodeMirror editor renders with a dark background and syntax-highlighted colors"
    - "The editor container background matches the dark theme (no white flash border)"
  artifacts:
    - path: "src/components/Editor.tsx"
      provides: "oneDark theme applied to CodeMirror extensions array"
  key_links:
    - from: "src/components/Editor.tsx"
      to: "@codemirror/theme-one-dark"
      via: "oneDark imported and spread into extensions array"
      pattern: "oneDark"
---

<objective>
Apply the CodeMirror 6 `oneDark` theme to the editor so it renders with a dark background and matching syntax colors.

Purpose: The editor currently has no color theme — it renders with a plain white background. A dark theme improves legibility and gives the app a polished look.
Output: Editor.tsx with `oneDark` applied; editor container div styled with a matching dark background color.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/Editor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @codemirror/theme-one-dark and apply oneDark theme to Editor</name>
  <files>src/components/Editor.tsx, package.json</files>
  <action>
1. Install the package:
   ```
   npm install @codemirror/theme-one-dark
   ```

2. In `src/components/Editor.tsx`, add the import at the top:
   ```typescript
   import { oneDark } from '@codemirror/theme-one-dark'
   ```

3. Add `oneDark` to the `extensions` array (insert it before the existing `EditorView.theme(...)` override so the size override still takes effect):
   ```typescript
   const extensions = [
     oneDark,                              // <-- add here
     markdown({ codeLanguages: languages }),
     EditorView.lineWrapping,
     lineNumbers(),
     highlightActiveLine(),
     drawSelection(),
     history(),
     keymap.of([...defaultKeymap, ...historyKeymap]),
     EditorView.updateListener.of((update) => {
       if (update.docChanged) {
         isInternalChange.current = true
         onChange(update.state.doc.toString())
       }
     }),
     EditorView.theme({
       '&': { height: '100%' },
       '.cm-scroller': { overflow: 'auto' },
     }),
   ]
   ```

4. Update the container div in the return statement to carry a matching dark background so there is no white flash while the editor mounts:
   ```tsx
   return <div ref={containerRef} className="h-full w-full bg-[#282c34]" />
   ```
   (`#282c34` is the oneDark editor background color.)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - `npm run build` exits 0 with no TypeScript or Vite errors
    - `@codemirror/theme-one-dark` appears in package.json dependencies
    - `oneDark` is imported and present in the extensions array in Editor.tsx
    - Container div has `bg-[#282c34]` class
  </done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0 with no errors. Optionally run `npm run dev` and confirm the editor pane renders with a dark background and colored markdown syntax highlighting.
</verification>

<success_criteria>
- Build passes (TypeScript + Vite) with zero errors
- Editor renders with oneDark dark background (#282c34) and syntax colors matching the One Dark palette
- No white flash on the container div before CodeMirror mounts
</success_criteria>

<output>
After completion, create `.planning/quick/260415-05x-add-dark-mode-styling-to-the-codemirror-/260415-05x-SUMMARY.md`
</output>
