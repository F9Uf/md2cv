---
phase: 12-repo-sync-toolbar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Dialog.tsx
  - src/components/ConflictModal.tsx
autonomous: true
requirements: [SYNC-03]
must_haves:
  truths:
    - "A reusable modal renders a titled panel over a dark backdrop"
    - "The modal closes on Escape and backdrop click when a close handler is provided"
    - "The conflict modal presents keep-local and use-remote choices and cannot be dismissed without choosing"
  artifacts:
    - path: "src/components/Dialog.tsx"
      provides: "Reusable modal primitive (backdrop + panel + a11y + dismissal)"
      exports: ["default"]
      min_lines: 40
    - path: "src/components/ConflictModal.tsx"
      provides: "Blocking keep-local / use-remote conflict prompt"
      exports: ["default"]
      min_lines: 30
  key_links:
    - from: "src/components/ConflictModal.tsx"
      to: "src/components/Dialog.tsx"
      via: "import + wrap"
      pattern: "from './Dialog'"
---

<objective>
Create the app's first modal primitive `Dialog.tsx` and the presentational `ConflictModal.tsx` (keep-local / use-remote). Both are pure presentational components driven by props — no GitHub API, no hooks beyond local effects. They establish the reusable dialog pattern the picker and commit dialogs (Plan 04) build on.

Purpose: The picker, conflict prompt, and commit dialog are all new modals; a shared `Dialog` primitive prevents three divergent implementations. The conflict modal (SYNC-03) is intentionally non-dismissible.
Output: `src/components/Dialog.tsx`, `src/components/ConflictModal.tsx`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-repo-sync-toolbar/12-UI-SPEC.md
@.planning/phases/12-repo-sync-toolbar/12-PATTERNS.md

<interfaces>
<!-- Outside-click + Escape close effect — extracted from src/components/Header.tsx lines 38-59 -->
```typescript
useEffect(() => {
  if (!open) return
  function handleMouseDown(e: MouseEvent) {
    if (panelRef.current && !panelRef.current.contains(e.target as Node)) onClose()
  }
  function handleKeyDown(e: KeyboardEvent) { if (e.key === 'Escape') onClose() }
  document.addEventListener('mousedown', handleMouseDown)
  document.addEventListener('keydown', handleKeyDown)
  return () => {
    document.removeEventListener('mousedown', handleMouseDown)
    document.removeEventListener('keydown', handleKeyDown)
  }
}, [open, onClose])
```

Button classes (from Header.tsx):
- Secondary/gray: `h-8 px-3 rounded bg-gray-700 text-white text-sm border border-gray-600 hover:bg-gray-600 transition-colors`
- Primary/blue: `h-8 px-3 rounded bg-blue-600 text-white text-sm border border-blue-500 hover:bg-blue-500 transition-colors`
- Destructive/red (full-width): `h-9 w-full rounded bg-red-800 text-white text-sm border border-red-700 hover:bg-red-700 transition-colors`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Dialog modal primitive</name>
  <files>src/components/Dialog.tsx</files>
  <read_first>
    - src/components/Header.tsx (lines 38-59 outside-click/Escape effect; lines 132-146 dropdown markup — the close pattern is the template)
    - .planning/phases/12-repo-sync-toolbar/12-UI-SPEC.md (Component 3 "Dialog/Modal Primitive" — exact backdrop + panel markup and a11y contract)
  </read_first>
  <action>
Create `src/components/Dialog.tsx`. Props interface:

```typescript
interface DialogProps {
  open: boolean
  title: string
  onClose?: () => void   // when undefined, Escape and backdrop-click do NOT dismiss (blocking modals pass no onClose)
  maxWidthClass?: string // default 'max-w-md'; picker passes 'max-w-lg'
  children: React.ReactNode
}
```

Behavior:
- If `!open`, return `null`.
- Generate a stable title id with React `useId()` and wire `aria-labelledby` to it.
- Backdrop: `<div className="fixed inset-0 bg-black/60 z-50 flex items-center justify-center p-4" role="dialog" aria-modal="true" aria-labelledby={titleId} onClick={handleBackdropClick}>`. `handleBackdropClick` calls `onClose?.()` only when `e.target === e.currentTarget` (click landed on the backdrop, not the panel).
- Panel: `<div ref={panelRef} onClick={(e) => e.stopPropagation()} className={\`bg-gray-900 border border-gray-600 rounded-lg shadow-xl w-full ${maxWidthClass ?? 'max-w-md'} p-6\`}>`.
- Title: `<h2 id={titleId} className="text-base font-semibold text-white mb-4">{title}</h2>`.
- Render `{children}` after the title.
- Escape handling: add the outside-click + Escape `useEffect` from Header.tsx lines 38-59, but guard `if (!open) return` and call `onClose?.()`. Only add the `keydown` (Escape) listener; backdrop click is already handled by `handleBackdropClick`. When `onClose` is undefined, `onClose?.()` is a no-op so the modal is non-dismissible (used by ConflictModal).
- Focus management: on open, move focus into the panel — `useEffect(() => { if (open) panelRef.current?.focus() }, [open])` with `tabIndex={-1}` on the panel so it is focusable.
  </action>
  <verify>
    <automated>cd /Users/charnsak/Desktop/project/md2cv && npx tsc -b --noEmit 2>&1 | head -20; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "role=\"dialog\"" src/components/Dialog.tsx` present
    - `grep "aria-modal=\"true\"" src/components/Dialog.tsx` present
    - `grep "aria-labelledby" src/components/Dialog.tsx` present
    - `grep "bg-black/60" src/components/Dialog.tsx` present (backdrop)
    - `grep "onClose?.()" src/components/Dialog.tsx` present (optional dismissal — non-dismissible when undefined)
    - `grep "e.target === e.currentTarget" src/components/Dialog.tsx` present (backdrop-only click dismiss)
    - `grep "if (!open) return" src/components/Dialog.tsx` present
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>Dialog renders a titled panel over a backdrop, dismisses on Escape/backdrop only when onClose is provided, and is fully typed.</done>
</task>

<task type="auto">
  <name>Task 2: Build the ConflictModal component</name>
  <files>src/components/ConflictModal.tsx</files>
  <read_first>
    - src/components/Dialog.tsx (the primitive just created — reuse it as the container)
    - .planning/phases/12-repo-sync-toolbar/12-UI-SPEC.md (Component 5 "Conflict Modal" — copy, button styling, stacking, no-dismiss rule)
  </read_first>
  <action>
Create `src/components/ConflictModal.tsx`. Props:

```typescript
interface ConflictModalProps {
  open: boolean
  onKeepLocal: () => void
  onUseRemote: () => void
}
```

Render `<Dialog open={open} title="Your local edits differ from GitHub">` — deliberately pass NO `onClose` so the modal is non-dismissible (user must choose).

Inside:
- Body: `<p className="text-sm text-gray-300 mb-6">Choose which version to keep. This cannot be undone.</p>`
- Two full-width buttons stacked vertically in `<div className="flex flex-col gap-2">`, safe option first:
  1. "Keep my local version" — `onClick={onKeepLocal}` — class `h-9 w-full rounded bg-gray-700 text-white text-sm border border-gray-600 hover:bg-gray-600 transition-colors`
  2. "Use GitHub version" — `onClick={onUseRemote}` — class `h-9 w-full rounded bg-red-800 text-white text-sm border border-red-700 hover:bg-red-700 transition-colors` (destructive)

Copy strings must be exactly as written above (from the UI-SPEC copywriting contract).
  </action>
  <verify>
    <automated>cd /Users/charnsak/Desktop/project/md2cv && npx tsc -b --noEmit 2>&1 | head -20; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from './Dialog'" src/components/ConflictModal.tsx` present
    - `grep "Your local edits differ from GitHub" src/components/ConflictModal.tsx` present (exact title)
    - `grep "Keep my local version" src/components/ConflictModal.tsx` present
    - `grep "Use GitHub version" src/components/ConflictModal.tsx` present
    - `grep "bg-red-800" src/components/ConflictModal.tsx` present (destructive styling on use-remote)
    - `grep -v "onClose" src/components/ConflictModal.tsx` — the file does NOT pass an onClose prop to Dialog (`grep "onClose" src/components/ConflictModal.tsx` returns nothing)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>ConflictModal wraps Dialog with no onClose, shows the exact conflict copy, and offers keep-local (gray) then use-remote (red) full-width buttons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → data-loss action | The "Use GitHub version" choice discards local edits; "Keep local" later overwrites remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Repudiation / accidental data loss | ConflictModal | mitigate | Non-dismissible modal forces an explicit choice (no Escape/backdrop); destructive option styled red and placed second so the safe option is the default reach; body text warns "This cannot be undone" |
| T-12-05 | Denial of Service (focus trap escape) | Dialog | accept | Lightweight focus-to-panel only (no full focus-trap library); personal single-user tool, low risk; Escape/backdrop dismissal covers the non-blocking dialogs |
</threat_model>

<verification>
- `npx tsc -b --noEmit` passes
- Both components render (verified via App wiring in Plan 06 and human-verify in Plan 07)
</verification>

<success_criteria>
A reusable Dialog primitive and a non-dismissible ConflictModal exist, typed and matching the UI-SPEC copy and styling.
</success_criteria>

<output>
After completion, create `.planning/phases/12-repo-sync-toolbar/12-02-SUMMARY.md`
</output>
