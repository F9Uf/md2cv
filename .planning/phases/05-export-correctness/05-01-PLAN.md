---
phase: 05-export-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/ExportTarget.tsx
  - src/lib/templateInlineStyles.ts
autonomous: true
requirements: [STYLE-05, HTML-03]

must_haves:
  truths:
    - "Exported PDF preserves bold, italic, inline code, and link formatting from bullet points"
    - "Exported PDF renders inline and block HTML content correctly"
    - "Link URLs are not appended after link text in printed output"
    - "Inline code has monospace font and gray background in printed output"
    - "ExportTarget.tsx and templateInlineStyles.ts no longer exist in the codebase"
  artifacts:
    - path: "src/index.css"
      provides: "Print CSS rules for inline elements"
      contains: "a[href]::after"
    - path: "src/index.css"
      provides: "Print CSS rules for code elements"
      contains: "#print-area code"
  key_links:
    - from: "src/index.css @media print"
      to: "#print-area a[href]::after"
      via: "CSS cascade"
      pattern: "content:\\s*none"
    - from: "src/index.css @media print"
      to: "#print-area code"
      via: "CSS cascade"
      pattern: "font-family:\\s*monospace"
---

<objective>
Add print-specific CSS for inline elements (links, code) and delete orphaned ExportTarget dead code.

Purpose: Ensure PDF export via window.print() renders inline styles and HTML correctly, matching the preview. Clean up dead code from the old html2pdf.js export path.
Output: Updated src/index.css with print CSS; ExportTarget.tsx and templateInlineStyles.ts deleted.
</objective>

<execution_context>
@$HOME/.claude-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-correctness/05-CONTEXT.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add print CSS for link URL suppression and inline code styling</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — read the entire file to see existing @media print block (lines 3-33)
  </read_first>
  <action>
Add the following rules INSIDE the existing `@media print { }` block in src/index.css, after the `body` rule (after line 32, before the closing `}`):

```css
  /* Suppress URL display after links in print */
  #print-area a[href]::after {
    content: none !important;
  }

  /* Style inline code in print */
  #print-area code {
    font-family: monospace;
    background-color: #f3f4f6;
    padding: 1px 3px;
    border-radius: 2px;
  }
```

Per D-01: suppress URL display for all links in print area.
Per D-02: inline code gets monospace font + subtle gray background across all templates.

Do NOT modify anything outside the @media print block. Do NOT touch any other file.
  </action>
  <verify>
    <automated>grep -n "content: none" /Users/charnsak/Desktop/md2cv/src/index.css && grep -n "#print-area code" /Users/charnsak/Desktop/md2cv/src/index.css && grep -n "font-family: monospace" /Users/charnsak/Desktop/md2cv/src/index.css && grep -n "background-color: #f3f4f6" /Users/charnsak/Desktop/md2cv/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "a\[href\]::after" src/index.css` returns a match inside @media print
    - `grep "content: none !important" src/index.css` returns a match
    - `grep "#print-area code" src/index.css` returns a match
    - `grep "font-family: monospace" src/index.css` returns a match
    - `grep "background-color: #f3f4f6" src/index.css` returns a match
    - `grep "padding: 1px 3px" src/index.css` returns a match
    - `grep "border-radius: 2px" src/index.css` returns a match
    - The @media print block still contains all original rules (display: none for #app-shell, etc.)
  </acceptance_criteria>
  <done>Print CSS suppresses link URLs and styles inline code with monospace font and gray background. All existing print rules preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned ExportTarget and templateInlineStyles files</name>
  <files>src/components/ExportTarget.tsx, src/lib/templateInlineStyles.ts</files>
  <read_first>
    - src/App.tsx — confirm no import of ExportTarget exists (it was already removed)
  </read_first>
  <action>
Per D-03: Delete these two orphaned files:

1. `rm src/components/ExportTarget.tsx`
2. `rm src/lib/templateInlineStyles.ts`

These files are dead code — the export flow uses window.print() via Preview, not html2pdf.js via ExportTarget. No imports reference these files from App.tsx or any other active source file.

After deletion, run `npx tsc --noEmit` to confirm no broken imports exist anywhere in the project.
  </action>
  <verify>
    <automated>test ! -f /Users/charnsak/Desktop/md2cv/src/components/ExportTarget.tsx && test ! -f /Users/charnsak/Desktop/md2cv/src/lib/templateInlineStyles.ts && echo "DELETED" && cd /Users/charnsak/Desktop/md2cv && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/components/ExportTarget.tsx` exits 0
    - `test ! -f src/lib/templateInlineStyles.ts` exits 0
    - `npx tsc --noEmit` produces no errors related to ExportTarget or templateInlineStyles
    - `grep -r "ExportTarget" src/` returns no matches
    - `grep -r "templateInlineStyles" src/` returns no matches
  </acceptance_criteria>
  <done>ExportTarget.tsx and templateInlineStyles.ts deleted. No broken imports in the project. Dead code removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries — CSS-only changes and file deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Info Disclosure) | Print CSS link suppression | mitigate | `content: none !important` on `a[href]::after` prevents URL leakage in printed output |
</threat_model>

<verification>
1. `grep -c "content: none" src/index.css` returns 1
2. `grep -c "#print-area code" src/index.css` returns at least 1
3. `test ! -f src/components/ExportTarget.tsx` exits 0
4. `test ! -f src/lib/templateInlineStyles.ts` exits 0
5. `npx tsc --noEmit` passes with no errors
6. `npm run build` succeeds
</verification>

<success_criteria>
- Print CSS in src/index.css suppresses link URL display and styles inline code
- ExportTarget.tsx and templateInlineStyles.ts are deleted
- Project compiles without errors
- PDF export via Ctrl+P / window.print() renders inline styles correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-correctness/05-01-SUMMARY.md`
</output>
