---
phase: 10-unified-pixel-perfect-pdf-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - PDFX-02
tags:
  - dependency-cleanup
  - documentation
  - tech-stack
must_haves:
  truths:
    - "The codebase carries no dead dependencies referencing the retired html2pdf.js pipeline — `html2pdf.js` and `@types/html2pdf.js` are gone from package.json and package-lock.json."
    - "Project docs (README.md, CLAUDE.md) describe the actual PDF pipeline — browser-native print over the paged.js DOM — not the historical html2pdf.js / jsPDF approach."
  artifacts:
    - path: "package.json"
      provides: "Cleaned dependency manifest with no html2pdf.js references."
      contains: "\"dompurify\""
    - path: "package-lock.json"
      provides: "Regenerated lockfile matching the cleaned package.json."
    - path: "README.md"
      provides: "Tech-stack bullet for PDF export reflects the print-CSS pipeline."
      contains: "paged.js"
    - path: "CLAUDE.md"
      provides: "Project block tech-stack line reflects the print-CSS pipeline."
      contains: "paged.js"
  key_links:
    - from: "package.json (dependencies)"
      to: "node_modules (resolved via npm install)"
      via: "regenerated package-lock.json"
      pattern: "package-lock.json"
    - from: "README.md `## Tech stack` bullet for PDF"
      to: "actual export mechanism (handleExportPdf → window.print → paged.js DOM)"
      via: "documentation accuracy"
      pattern: "paged.js"
---

<objective>
Close the loop on the retired html2pdf.js pipeline by removing both deps from `package.json` (and the regenerated `package-lock.json`), and updating `README.md` line 32 plus `CLAUDE.md` line 14 so the published tech-stack description matches the reality after Phase 10 (browser-native print over a paged.js-rendered DOM, no PDF library).

Purpose: PDFX-02 (single unified rendering path). Per CONTEXT.md D-05, these deps have been dead since Phase 6 — Phase 10 is the cleanup phase.

Output: A `package.json` with no `html2pdf.js` entry, a regenerated `package-lock.json`, and updated docs. `npm run build` still passes.

This plan is INDEPENDENT of Plan 01 (no file overlap, no logical dependency): the deps were already unused before Phase 10 began, so removing them does not require the Plan 01 render-pipeline changes to land first. Both plans live in Wave 1 and may execute in parallel.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-CONTEXT.md
@.planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-PATTERNS.md
@package.json
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove html2pdf.js and @types/html2pdf.js from package.json + regenerate package-lock.json</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (entire file — 48 lines)
    - .planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-CONTEXT.md `<decisions>` D-05
    - .planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-PATTERNS.md section "package.json"
  </read_first>
  <action>
Confirm via grep that no source file imports `html2pdf.js`:

```bash
grep -rn "html2pdf" src/ || echo "no usages — safe to remove"
```

(Expected: zero matches in `src/` — the deps have been dead since Phase 6. If any match exists, STOP and report; do not remove the dep.)

Edit `package.json`:
1. In `"dependencies"`, delete the line `"html2pdf.js": "^0.14.0",` (currently line 24).
2. In `"devDependencies"`, delete the line `"@types/html2pdf.js": "^0.10.0",` (currently line 34).
3. Preserve trailing-comma JSON validity (delete the comma after the previous line if `html2pdf.js` was the last entry in its block — it is not; `markdown-it` follows it in deps, and `@types/markdown-it` follows `@types/html2pdf.js` in devDeps — so simple line deletion works).
4. Preserve alphabetical ordering of remaining entries (already correct after the deletion: `dompurify`, `markdown-it`, `pagedjs` in deps; `@types/dompurify`, `@types/markdown-it` in devDeps).

Regenerate the lockfile:

```bash
npm install
```

This rewrites `package-lock.json` to drop the html2pdf.js transitive tree. No other version bumps should occur — verify by running `git diff package-lock.json | head -50` and confirming the diff is limited to html2pdf.js / its transitives (typically `html2canvas`, `es6-promise`, `jspdf`).

Then run a build to confirm nothing imports the removed dep:

```bash
npm run build
```

Must succeed with zero errors.
  </action>
  <verify>
    <automated>! grep -F 'html2pdf.js' package.json && ! grep -F 'html2pdf.js' package-lock.json && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -F 'html2pdf.js' package.json` (the dep is gone from the manifest).
    - `! grep -F '@types/html2pdf.js' package.json` (the type dep is gone).
    - `! grep -F 'html2pdf.js' package-lock.json` (the lockfile no longer references the package).
    - `grep -F '"dompurify"' package.json` still returns a match (adjacent dep preserved).
    - `grep -F '"pagedjs"' package.json` still returns a match (adjacent dep preserved).
    - `grep -F '"markdown-it"' package.json` still returns a match.
    - `grep -F '"@types/markdown-it"' package.json` still returns a match.
    - `grep -rn 'html2pdf' src/ | wc -l` returns `0` (no source usages — should have been zero before this task too; verifies nothing was broken).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    `package.json` and `package-lock.json` contain no reference to `html2pdf.js` or `@types/html2pdf.js`. `npm run build` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md tech-stack PDF line</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file — 50 lines; the target is line 32 in the `## Tech stack` block)
    - .planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-CONTEXT.md `<decisions>` D-05 and Claude's Discretion bullet on README/CLAUDE wording
    - .planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-PATTERNS.md section "README.md"
  </read_first>
  <action>
In `README.md`, replace line 32:

```markdown
- **html2pdf.js / jsPDF** — client-side PDF export
```

with:

```markdown
- **paged.js + browser print** — paginated DOM rendered in-browser, exported via the browser's native Save-as-PDF
```

Do NOT modify any other line in `README.md`. The other bullets in the `## Tech stack` section (lines 25-31) stay verbatim. The bullet count and overall section structure stay the same — this is a single-line replacement.
  </action>
  <verify>
    <automated>! grep -F 'html2pdf.js' README.md && grep -F 'paged.js + browser print' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -F 'html2pdf.js' README.md` (the old library mention is gone).
    - `! grep -F 'jsPDF' README.md` (the alternative library mention is also gone).
    - `grep -F 'paged.js + browser print' README.md` returns exactly 1 match.
    - `grep -F '**markdown-it**' README.md` still returns 1 match (adjacent bullet preserved).
    - `grep -F '**CodeMirror**' README.md` still returns 1 match (adjacent bullet preserved).
    - `grep -c '^- \*\*' README.md` returns at least 6 (same number of tech-stack bullets — count unchanged).
  </acceptance_criteria>
  <done>
    `README.md` tech-stack bullet for PDF export reflects the actual pipeline. No other line modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md tech-stack line (inside GSD project block)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (entire file — focus lines 1-15, the `<!-- GSD:project-start source:PROJECT.md -->` block)
    - .planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-CONTEXT.md `<decisions>` D-05 and Claude's Discretion bullet on CLAUDE.md
    - .planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-PATTERNS.md section "CLAUDE.md"
  </read_first>
  <action>
In `CLAUDE.md`, replace line 14:

```markdown
- **Tech stack**: markdown-it for parsing, CodeMirror for editor, html2pdf.js or jsPDF for PDF export
```

with:

```markdown
- **Tech stack**: markdown-it for parsing, CodeMirror for editor, paged.js for pagination, browser print for PDF export
```

Do NOT modify any other line in `CLAUDE.md`. Do NOT modify the surrounding GSD content-block markers (`<!-- GSD:project-start source:PROJECT.md -->` … `<!-- GSD:project-end -->`).

Note (D-05 + Claude's Discretion): the source of truth for this block is `.planning/PROJECT.md` and a future `generate-claude-profile` run may regenerate `CLAUDE.md` from there. CONTEXT.md explicitly defers `PROJECT.md` updates to the end-of-phase transition step, so editing `CLAUDE.md` directly here is the chosen path. If the regenerator overwrites this edit later, the transition step will re-apply the equivalent change to `PROJECT.md`. Accept the risk of a short-lived divergence per Claude's Discretion in CONTEXT.md.
  </action>
  <verify>
    <automated>! grep -F 'html2pdf.js or jsPDF' CLAUDE.md && grep -F 'paged.js for pagination, browser print for PDF export' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -F 'html2pdf.js or jsPDF' CLAUDE.md` (the historical phrase is gone).
    - `grep -F 'paged.js for pagination, browser print for PDF export' CLAUDE.md` returns exactly 1 match.
    - `grep -F 'markdown-it for parsing' CLAUDE.md` still returns 1 match (preserved verbatim).
    - `grep -F 'CodeMirror for editor' CLAUDE.md` still returns 1 match (preserved verbatim).
    - `grep -F 'GSD:project-start' CLAUDE.md` returns exactly 1 match (block marker preserved).
    - `grep -F 'GSD:project-end' CLAUDE.md` returns exactly 1 match (block marker preserved).
  </acceptance_criteria>
  <done>
    `CLAUDE.md` line 14 (inside the GSD project block) reflects the new pipeline. Block markers are intact. No other line modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm registry | Removing deps narrows the resolved dependency tree; supply-chain surface shrinks. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | T (Tampering) / Supply chain | npm dep tree | accept | No new threat surface — print-pipeline refactor only (dep-removal sub-plan). Removing `html2pdf.js` and `@types/html2pdf.js` reduces the supply-chain surface; no deps are added. Lockfile regeneration is bounded to the removed package and its transitives — operator verifies the diff scope with `git diff package-lock.json` before committing. |
</threat_model>

<verification>
After this plan, run:

```bash
npm run build && npm run lint
git diff --stat package.json package-lock.json README.md CLAUDE.md
```

`npm run build` must pass. `npm run lint` must pass. The git diff should show exactly the four files in `files_modified` changed.
</verification>

<success_criteria>
- `package.json` carries neither `html2pdf.js` nor `@types/html2pdf.js`.
- `package-lock.json` regenerated to match; no other dep version bumped.
- `README.md` line 32 tech-stack bullet reflects "paged.js + browser print".
- `CLAUDE.md` line 14 tech-stack bullet reflects "paged.js for pagination, browser print for PDF export".
- `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-pixel-perfect-pdf-pipeline/10-02-SUMMARY.md` capturing:
- The exact deps removed with their pre-removal versions.
- The transitive packages that disappeared from `package-lock.json` (read off `git diff package-lock.json`).
- A note that PROJECT.md update for the tech-stack line is deferred to the end-of-phase transition step per CONTEXT.md `<deferred>`.
</output>
