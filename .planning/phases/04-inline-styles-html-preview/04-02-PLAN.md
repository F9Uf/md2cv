---
phase: 04-inline-styles-html-preview
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/Preview.tsx
  - src/components/ExportTarget.tsx
autonomous: false
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - HTML-01
  - HTML-02

must_haves:
  truths:
    - "Bold text in a bullet renders as <strong> in the preview (not raw **bold**)"
    - "Italic text in a bullet renders as <em> in the preview (not raw *italic*)"
    - "Inline code in a bullet renders as <code> in the preview (not raw `code`)"
    - "Links in a bullet render as <a href> in the preview (not raw [text](url))"
    - "Inline HTML written directly in markdown (e.g. <br>, <span>) renders in the preview"
    - "Block HTML written in markdown (e.g. <div>, <table>) renders in the preview"
  artifacts:
    - path: "src/components/Preview.tsx"
      provides: "Preview using dangerouslySetInnerHTML for detail list items"
      contains: "dangerouslySetInnerHTML"
    - path: "src/components/ExportTarget.tsx"
      provides: "ExportTarget using dangerouslySetInnerHTML for detail list items"
      contains: "dangerouslySetInnerHTML"
  key_links:
    - from: "src/components/Preview.tsx"
      to: "resumeData.sections[].entries[].details[]"
      via: "dangerouslySetInnerHTML={{ __html: detail }}"
      pattern: "__html.*detail"
    - from: "src/components/ExportTarget.tsx"
      to: "resumeData.sections[].entries[].details[]"
      via: "dangerouslySetInnerHTML={{ __html: detail }}"
      pattern: "__html.*detail"
---

<objective>
Update Preview.tsx and ExportTarget.tsx to render bullet detail items as HTML using dangerouslySetInnerHTML, consuming the HTML strings now produced by the updated parser (Plan 01).

Purpose: The parser now stores details as rendered HTML. Without this plan, the preview still shows raw HTML strings (e.g. `<strong>bold</strong>`) as text. After this plan, formatted text appears correctly.

Output: Preview.tsx and ExportTarget.tsx render `<li dangerouslySetInnerHTML={{ __html: detail }}>` instead of `<li>{detail}</li>`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-inline-styles-html-preview/04-01-SUMMARY.md
@src/components/Preview.tsx
@src/components/ExportTarget.tsx
@src/types/resume.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render detail list items as HTML in Preview and ExportTarget</name>
  <files>src/components/Preview.tsx, src/components/ExportTarget.tsx</files>
  <read_first>
    - src/components/Preview.tsx — read lines 50–58; the current `<li key={di} className={styles.entryDetail}>{detail}</li>` is what to change
    - src/components/ExportTarget.tsx — read lines 59–64; the current `<li key={di} style={s.entryDetail}>{detail}</li>` is what to change
    - src/types/resume.ts — confirm `details: string[]` type (unchanged, but values are now HTML strings from Plan 01)
  </read_first>
  <action>
In `src/components/Preview.tsx`:

Change the `<li>` element that renders each detail item from:
```tsx
<li key={di} className={styles.entryDetail}>{detail}</li>
```
to:
```tsx
<li key={di} className={styles.entryDetail} dangerouslySetInnerHTML={{ __html: detail }} />
```
The element becomes self-closing when using dangerouslySetInnerHTML (cannot have children).

In `src/components/ExportTarget.tsx`:

Change the `<li>` element that renders each detail item from:
```tsx
<li key={di} style={s.entryDetail}>{detail}</li>
```
to:
```tsx
<li key={di} style={s.entryDetail} dangerouslySetInnerHTML={{ __html: detail }} />
```

These are the only two changes needed. The `extra` fields already use `dangerouslySetInnerHTML` — this aligns `details` with that established pattern.
  </action>
  <verify>
    <automated>grep -n "dangerouslySetInnerHTML" /Users/charnsak/Desktop/md2cv/src/components/Preview.tsx && grep -n "dangerouslySetInnerHTML" /Users/charnsak/Desktop/md2cv/src/components/ExportTarget.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" src/components/Preview.tsx` returns 4 or more (was 2 for preamble/extra; now also includes detail li)
    - `grep -c "dangerouslySetInnerHTML" src/components/ExportTarget.tsx` returns 4 or more (same reasoning)
    - `grep "__html: detail" src/components/Preview.tsx` returns a match
    - `grep "__html: detail" src/components/ExportTarget.tsx` returns a match
    - `npx tsc --noEmit` exits 0
    - No `{detail}` (plain text rendering of detail) remains in either file: `grep -n ">{detail}<" src/components/Preview.tsx` returns no match; same for ExportTarget
  </acceptance_criteria>
  <done>Both Preview and ExportTarget render bullet detail items via dangerouslySetInnerHTML. Plain text rendering of details is eliminated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Parser now renders bullet detail items as HTML (Plan 01: md.renderInline + html:true).
    Preview and ExportTarget now render those HTML strings with dangerouslySetInnerHTML (Task 1 above).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. In the editor, write a bullet list under any h3 entry with:
       ```
       - **bold** and *italic* text
       - Use `code` inline
       - [Link text](https://example.com)
       - <span style="color:red">Red text via HTML</span>
       ```
    3. Verify in the preview:
       - "bold" appears bold (not `**bold**`)
       - "italic" appears italic (not `*italic*`)
       - "code" appears in monospace/code style (not backticks)
       - "Link text" appears as a clickable link (not `[Link text](...)`)
       - "Red text via HTML" appears in red (not as escaped HTML string)
    4. Also add a block HTML element anywhere in the markdown, e.g. a `<table>` before an h2, and verify it renders as a table, not escaped text.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 bullet items render correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resumeData.details[] → dangerouslySetInnerHTML | HTML strings from parser rendered into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | dangerouslySetInnerHTML for detail items | accept | Personal single-user tool; content is user's own markdown; XSS sanitization explicitly out of scope per REQUIREMENTS.md |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep "__html: detail" src/components/Preview.tsx` returns a match
- `grep "__html: detail" src/components/ExportTarget.tsx` returns a match
- Human verification: bold/italic/code/links in bullets render correctly in the browser preview
</verification>

<success_criteria>
- Preview.tsx `<li>` for details uses dangerouslySetInnerHTML
- ExportTarget.tsx `<li>` for details uses dangerouslySetInnerHTML
- TypeScript compiles without errors
- Human confirms visual rendering is correct (checkpoint passed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-inline-styles-html-preview/04-02-SUMMARY.md`
</output>
