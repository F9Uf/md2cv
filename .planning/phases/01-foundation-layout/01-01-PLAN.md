---
phase: 01-foundation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - tailwind.config.js
  - postcss.config.js
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/components/Header.tsx
autonomous: true
requirements:
  - LAYO-01

must_haves:
  truths:
    - "Vite dev server starts and renders the React app at localhost"
    - "Tailwind utility classes apply styles correctly"
    - "Header bar is visible with md2cv branding and placeholder slots"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "react"
    - path: "src/App.tsx"
      provides: "Root app component with shell layout"
      exports: ["default"]
    - path: "src/components/Header.tsx"
      provides: "Header bar with branding and placeholder slots"
      exports: ["default"]
    - path: "tailwind.config.js"
      provides: "Tailwind CSS configuration"
      contains: "content"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "import App"
    - from: "src/App.tsx"
      to: "src/components/Header.tsx"
      via: "component import"
      pattern: "import Header"
---

<objective>
Scaffold the md2cv project with Vite + React + Tailwind CSS (per D-01, D-02) and create the app shell with a header bar containing branding and placeholder slots for future controls (per D-03, D-04).

Purpose: Establish the greenfield project foundation that all subsequent phases build upon.
Output: A running Vite dev server rendering a React app with Tailwind CSS and a visible header bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-layout/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript + Tailwind CSS project</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, tailwind.config.js, postcss.config.js, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css</files>
  <read_first>
    - .planning/phases/01-foundation-layout/01-CONTEXT.md (decisions D-01, D-02)
  </read_first>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` in the project root to scaffold a React + TypeScript project with Vite (per D-01). If prompted about existing files, proceed (only CLAUDE.md and .planning exist).

    2. Run `npm install` to install base dependencies.

    3. Install Tailwind CSS v4 and its Vite plugin (per D-02):
       `npm install tailwindcss @tailwindcss/vite`

    4. Update `vite.config.ts` to include the Tailwind plugin:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'

       export default defineConfig({
         plugins: [
           react(),
           tailwindcss(),
         ],
       })
       ```

    5. Replace the contents of `src/index.css` with the Tailwind v4 import:
       ```css
       @import "tailwindcss";
       ```

    6. Update `src/App.tsx` to a minimal shell that confirms Tailwind works:
       ```tsx
       import './App.css'

       function App() {
         return (
           <div className="min-h-screen bg-gray-50 flex flex-col">
             <main className="flex-1 flex">
               <div className="flex-1 bg-white border-r border-gray-200 p-4">
                 <p className="text-gray-400">Editor pane (Phase 2)</p>
               </div>
               <div className="flex-1 bg-white p-4">
                 <p className="text-gray-400">Preview pane (Phase 2)</p>
               </div>
             </main>
           </div>
         )
       }

       export default App
       ```

    7. Clear `src/App.css` to be empty (all styling via Tailwind utilities).

    8. Verify the dev server starts: `npm run dev` — confirm no errors in terminal output.
  </action>
  <verify>
    <automated>cd /Users/charnsak/Desktop/md2cv && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react" and "tailwindcss" in dependencies
    - vite.config.ts contains "tailwindcss()" in plugins array
    - src/index.css contains "@import \"tailwindcss\""
    - src/App.tsx contains "min-h-screen" (Tailwind class confirming integration)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Vite dev server builds successfully, React app renders, Tailwind utility classes are functional</done>
</task>

<task type="auto">
  <name>Task 2: Create Header component with branding and placeholder slots</name>
  <files>src/components/Header.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (current state from Task 1)
    - .planning/phases/01-foundation-layout/01-CONTEXT.md (decisions D-03, D-04)
  </read_first>
  <action>
    1. Create `src/components/Header.tsx` (per D-03, D-04):
       ```tsx
       export default function Header() {
         return (
           <header className="h-12 bg-gray-900 text-white flex items-center justify-between px-4 shrink-0">
             <h1 className="text-lg font-bold tracking-tight">md2cv</h1>
             <div className="flex items-center gap-2">
               {/* Placeholder: template switcher (Phase 2) */}
               <div className="h-8 w-24 rounded bg-gray-700" aria-label="Template switcher placeholder" />
               {/* Placeholder: export button (Phase 3) */}
               <div className="h-8 w-20 rounded bg-gray-700" aria-label="Export button placeholder" />
             </div>
           </header>
         )
       }
       ```

    2. Update `src/App.tsx` to import and render Header above the main content area:
       ```tsx
       import './App.css'
       import Header from './components/Header'

       function App() {
         return (
           <div className="h-screen bg-gray-50 flex flex-col overflow-hidden">
             <Header />
             <main className="flex-1 flex min-h-0">
               <div className="flex-1 bg-white border-r border-gray-200 p-4">
                 <p className="text-gray-400">Editor pane (Phase 2)</p>
               </div>
               <div className="flex-1 bg-white p-4">
                 <p className="text-gray-400">Preview pane (Phase 2)</p>
               </div>
             </main>
           </div>
         )
       }

       export default App
       ```

    The header uses `h-12` fixed height, dark background (`bg-gray-900`), white text. The "md2cv" title is left-aligned (per D-04). Two placeholder divs on the right reserve space for template switcher and export button (per D-03), styled as gray rounded blocks so the layout is established.
  </action>
  <verify>
    <automated>cd /Users/charnsak/Desktop/md2cv && grep -l "md2cv" src/components/Header.tsx && grep -l "Header" src/App.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Header.tsx exists and contains "md2cv" as text content
    - src/components/Header.tsx contains "Template switcher placeholder" aria-label
    - src/components/Header.tsx contains "Export button placeholder" aria-label
    - src/App.tsx contains "import Header from './components/Header'"
    - src/App.tsx contains "<Header />"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Header bar renders with "md2cv" branding and visible placeholder slots for template switcher and export button</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Pure scaffold — no user input, no data processing, no external resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependencies | accept | Standard npm install from registry; no elevated-risk packages. Lock file committed. |
| T-01-02 | Information Disclosure | Source code | accept | Client-side SPA — all source visible by design. No secrets in code. |
</threat_model>

<verification>
- `npm run build` completes without errors
- Built output contains Header with "md2cv" branding
- Tailwind classes are compiled into output CSS
</verification>

<success_criteria>
- Vite + React + TypeScript project scaffolded and builds cleanly
- Tailwind CSS v4 integrated and utility classes functional
- Header component renders with "md2cv" title and two placeholder slots
- App shell has full-height layout with flex column structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-01-SUMMARY.md`
</output>
