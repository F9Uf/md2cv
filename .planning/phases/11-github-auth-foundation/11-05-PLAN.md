---
phase: 11-github-auth-foundation
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: github-oauth-app
    why: "The OAuth web flow requires a registered GitHub OAuth App (client id + secret) and a callback URL matching the deploy origin."
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub -> Settings -> Developer settings -> OAuth Apps -> New OAuth App -> Client ID (server env)"
      - name: GITHUB_CLIENT_SECRET
        source: "Same OAuth App -> Generate a new client secret (server env — Vercel project settings)"
      - name: VITE_GITHUB_CLIENT_ID
        source: "Same Client ID value, set as a VITE_ var for the client build"
      - name: VITE_AUTH_ENDPOINT
        source: "The deployed function URL, e.g. https://<project>.vercel.app/api/github-auth"
    dashboard_config:
      - task: "Create an OAuth App with Authorization callback URL = the app root (e.g. https://<project>.vercel.app/ for prod, http://localhost:5173/ for local dev)"
        location: "GitHub -> Settings -> Developer settings -> OAuth Apps"
      - task: "Set GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, VITE_GITHUB_CLIENT_ID, VITE_AUTH_ENDPOINT in the Vercel project (and a local .env for dev)"
        location: "Vercel -> Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "A real user completes the GitHub OAuth web flow and returns authenticated (AUTH-01)"
    - "The signed-in GitHub avatar appears in the header, proving a valid token (Success Criterion 2)"
    - "Sign out clears the token and returns the app to signed-out (AUTH-03)"
    - "Changing VITE_AUTH_ENDPOINT changes the exchange host without editing source (AUTH-02)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the end-to-end OAuth flow against a real GitHub OAuth App and the deployed (or locally-run) serverless function. This is the only step that requires real credentials and a live redirect, so it is a checkpoint rather than automated code.

Purpose: Confirm all four Phase 11 success criteria hold with real infrastructure before the phase is marked complete.
Output: user confirmation (no files changed).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-github-auth-foundation/11-CONTEXT.md
@.planning/ROADMAP.md
@.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 11-01 through 11-04 delivered: the serverless token-exchange function (`api/github-auth.ts`), the tested OAuth lib (`src/lib/githubAuth.ts`), the `useGitHubAuth` hook, and the header auth UI (sign-in button, avatar, dropdown, error toast). Everything typechecks and the lib unit tests pass. What Claude cannot do autonomously: register a GitHub OAuth App, supply the real client id/secret, and drive a live browser redirect.
  </what-built>
  <how-to-verify>
    One-time setup (see `user_setup` / `.env.example`):
    1. Create a GitHub OAuth App. Set the Authorization callback URL to your app root:
       - Local: `http://localhost:5173/`
       - Prod: `https://<your-project>.vercel.app/`
    2. Put the values in `.env` (local) and Vercel env settings (prod):
       - `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` (server)
       - `VITE_GITHUB_CLIENT_ID` (= same client id), `VITE_AUTH_ENDPOINT` (= your `/api/github-auth` URL)
    3. Run the app with the function available (`vercel dev`, or deploy to Vercel).

    Then verify each success criterion:
    - [ ] SC1 (AUTH-01): Click "Sign in with GitHub" -> you are redirected to GitHub, approve, and return to the app root. The button briefly shows the "Signing in..." spinner, then an avatar appears. The URL no longer contains `?code&state` (it was cleaned).
    - [ ] SC2: The header shows your GitHub avatar. Click it -> the dropdown shows your username and a "Sign out" item.
    - [ ] SC3 (AUTH-03): Click "Sign out" -> the avatar reverts to the "Sign in with GitHub" button. Reload the page: you stay signed out (DevTools -> Application -> Local Storage: `md2cv-github-token` is gone).
    - [ ] SC4 (AUTH-02): Confirm you did NOT edit any source file to point at your endpoint — only `VITE_AUTH_ENDPOINT` was set. (Optional: change it to a different host + rebuild and confirm the exchange follows.)
    - [ ] Failure path: Start sign-in, then click "Cancel" on GitHub's consent screen -> you return and see the red dismissible error toast below the header; the sign-in button is still usable to retry.
    - [ ] Persistence: After signing in, reload the page -> you remain signed in (avatar shown), confirming the localStorage token + GET /user validation (D-06).
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe which step failed (include any console/network errors).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub OAuth App config -> live redirect | Callback URL must match the deploy origin exactly, or GitHub rejects the redirect |
| Vercel env -> serverless runtime | Secrets provided via platform env, not source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Spoofing | OAuth callback URL | mitigate | Callback URL registered in the GitHub OAuth App must equal the app origin; mismatched redirects are rejected by GitHub. Verified during this checkpoint. |
| T-11-17 | Information Disclosure | secrets in Vercel env | mitigate | `GITHUB_CLIENT_SECRET` lives only in Vercel env / local `.env` (git-ignored per 11-01), never in the client bundle. Verified: only VITE_ vars appear in the built client. |
</threat_model>

<verification>
- Human confirms SC1-SC4, the failure toast, and reload persistence all behave as specified.
</verification>

<success_criteria>
- All four ROADMAP Phase 11 success criteria pass against real GitHub OAuth infrastructure.
- No secret is present in the client bundle; endpoint is config-driven (AUTH-02).
</success_criteria>

<output>
After approval, create `.planning/phases/11-github-auth-foundation/11-05-SUMMARY.md` recording the verification outcome.
Per project memory: after a human-verify checkpoint plan is approved, do NOT spawn the verifier — proceed straight to phase completion.
</output>
